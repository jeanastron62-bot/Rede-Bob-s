Aplica, mostrando o diff de cada arquivo antes de eu confirmar, nesta ordem:

1. users.service.ts: adiciona a mesma proteção de conta que já existe em
   updateRole/deleteUser dentro de approveUser (bloquear quando
   approved === false e username === 'tecnico' ou id === 1). E troca as
   4 ocorrências de reqUser.id por reqUser.userId no mesmo arquivo.

2. config.service.ts: troca o findUnique + create solto em getConfig()
   por upsert where: {id: 1}, igual ao padrão já usado em updateConfig().

3. orders.service.ts: quando newStatus === 'CANCELADO', zera
   requiresStaffConfirmation para false também, não só o status — hoje
   um pedido de mesa cancelado antes da confirmação fica preso para
   sempre na aba "Pedidos do Site".

4. seedData/menuItems.ts: remove os 3 sabores de suco fixos (Manga,
   Maracujá, Morango) — contraria decisão já documentada.

Depois desses 4 aplicados e eu confirmar, atualiza o CONTEXTO.md nos 4
pontos que você já identificou como desatualizados (frontend Fases 5-8
já implementadas, CORS resolvido, comportamento real do from/to em
ADM/TI, janela de expediente). Não mexe no Crítico 2 (dinheiro float no
frontend) nem começa a Fase 9 ainda — paro pra decidir isso depois de
ver os 4 primeiros funcionando.